# 跨市场套利监控系统 - 需求确认文档

## 📋 文档信息
- **项目名称**: Predict.fun × Polymarket 跨市场套利监控系统
- **文档版本**: v1.0
- **日期**: 2025年

---

## 一、项目概述

### 1.1 项目目标
开发一个基于 **Predict.fun** 和 **Polymarket** 的跨市场套利监控系统，实现：
- 实时获取两个预测市场的价格数据
- 自动识别同一事件的定价差异（套利机会）
- 量化套利空间和收益率
- 提供直观的监控界面

### 1.2 核心业务逻辑
```
┌─────────────────────────────────────────────────────────────────┐
│                      套利监控核心流程                            │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐   │
│  │ Predict.fun  │      │   市场匹配   │      │  Polymarket  │   │
│  │   数据获取   │─────▶│   与关联     │◀─────│   数据获取   │   │
│  └──────────────┘      └──────┬───────┘      └──────────────┘   │
│                               │                                  │
│                               ▼                                  │
│                        ┌──────────────┐                         │
│                        │  价格对比与  │                         │
│                        │  套利计算    │                         │
│                        └──────┬───────┘                         │
│                               │                                  │
│                               ▼                                  │
│                        ┌──────────────┐                         │
│                        │  套利机会    │                         │
│                        │  展示与通知  │                         │
│                        └──────────────┘                         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、功能需求

### 2.1 功能模块清单 (按优先级)

#### P0 - 必须有 (MVP核心功能)

| 模块 | 功能描述 | 关键特性 |
|------|----------|----------|
| **后端代理服务器** | 解决CORS限制，统一API代理 | 请求转发、认证头注入、响应缓存 |
| **市场数据获取** | 分页获取两个平台的全部市场 | Cursor分页、数据标准化、搜索筛选 |
| **订单簿数据获取** | 获取价格深度数据 | 互补价格计算(1-Yes价格)、批量获取 |
| **市场匹配引擎** | 识别同一事件的不同平台市场 | 标题相似度、关键词匹配、时间窗口 |
| **套利机会识别** | 计算并识别套利机会 | ROI计算、阈值过滤、风险评估 |
| **前端展示界面** | 市场列表、套利面板、详情页 | 表格/卡片双视图、实时更新 |

#### P1 - 应该有 (增强功能)

| 模块 | 功能描述 | 关键特性 |
|------|----------|----------|
| **实时监控引擎** | 定时刷新和变化检测 | 30秒自动刷新、WebSocket支持 |
| **通知系统** | 套利机会提醒 | 浏览器通知、声音提醒、阈值配置 |
| **数据持久化** | 历史数据存储 | 价格历史、套利历史、用户配置 |
| **设置页面** | 系统配置 | API Key管理、监控参数、显示偏好 |

#### P2 - 可以有 (高级功能)

| 模块 | 功能描述 | 关键特性 |
|------|----------|----------|
| **数据分析** | 统计分析和报告 | 套利分布、收益率统计、市场对比 |
| **历史记录** | 套利机会追踪 | 机会生命周期、成功率分析 |

### 2.2 套利识别逻辑

#### 套利条件
```
有效套利 = Yes价格 + No价格 < 1
ROI = (1 - 总成本) / 总成本
净收益率 = ROI - 平台手续费
```

#### 默认阈值配置
| 参数 | 默认值 | 可调范围 |
|------|--------|----------|
| 最小收益率 (MIN_ROI) | 1.5% | 0.5% - 5% |
| 最小利润额 | $5 | $1 - $50 |
| 最小深度 | $100 | $50 - $500 |
| 通知冷却时间 | 300秒 | 60-600秒 |

---

## 三、用户界面设计

### 3.1 整体布局

```
┌─────────────────────────────────────────────────────────────────┐
│  [Logo] 跨市场套利监控                    [刷新] [设置] [帮助]   │  ← Header (60px)
├──────────┬──────────────────────────────────────────────────────┤
│          │                                                      │
│  导航栏   │                    主内容区                          │
│ (240px)  │                  (自适应宽度)                         │
│          │                                                      │
│  ┌────┐  │  ┌────────────────────────────────────────────────┐  │
│  │市场│  │  │           筛选栏 / 搜索栏                       │  │
│  │列表│  │  └────────────────────────────────────────────────┘  │
│  ├────┤  │  ┌────────────────────────────────────────────────┐  │
│  │套利│  │  │                                                │  │
│  │机会│  │  │              数据展示区域                       │  │
│  ├────┤  │  │           (表格/卡片/图表)                      │  │
│  │设置│  │  │                                                │  │
│  └────┘  │  └────────────────────────────────────────────────┘  │
│          │  ┌────────────────────────────────────────────────┐  │
│          │  │              分页 / 加载更多                    │  │
│          │  └────────────────────────────────────────────────┘  │
└──────────┴──────────────────────────────────────────────────────┘
```

### 3.2 关键页面设计

#### 页面清单
| 页面 | 功能 | 核心组件 |
|------|------|----------|
| **市场列表页** | 查看所有市场数据 | 表格/卡片双视图、多维度筛选、分页 |
| **套利机会页** | 发现套利机会 | 分级高亮、实时更新、一键跳转 |
| **市场详情页** | 单个市场完整信息 | 价格走势图、双平台对比、套利分析 |
| **设置页** | 系统配置 | API Key配置、监控参数、显示偏好 |

#### 套利机会分级展示
| 级别 | 利润率 | 视觉标识 |
|------|--------|----------|
| 高利润 | ≥15% | 🔴 红色边框 + 发光效果 |
| 中利润 | 5%-15% | 🟠 橙色边框 |
| 低利润 | 2%-5% | 🟡 黄色标签 |
| 微利润 | <2% | ⚪ 灰色显示 |

### 3.3 响应式设计

| 断点 | 布局调整 |
|------|----------|
| ≥1400px | 完整布局，侧边栏展开 |
| 1024px-1399px | 侧边栏可折叠，内容区自适应 |
| 768px-1023px | 侧边栏隐藏为汉堡菜单 |
| <768px | 单列布局，卡片堆叠 |

### 3.4 视觉设计规范

#### 颜色系统
```css
/* 功能色 */
--success: #52c41a;    /* 盈利/上涨 */
--error: #ff4d4f;      /* 亏损/下跌 */
--warning: #faad14;    /* 警告 */
--info: #1890ff;       /* 信息 */

/* 平台标识色 */
--polymarket: #6B46C1; /* Polymarket紫色 */
--predictfun: #10B981; /* Predict.fun绿色 */
```

#### 字体层级
| 元素 | 大小 | 字重 |
|------|------|------|
| 套利利润率 | 24px | 700 (最突出) |
| 当前价格 | 18px | 600 |
| 市场名称 | 16px | 500 |
| 辅助信息 | 14px | 400 |
| 元数据 | 12px | 400 |

---

## 四、技术规格

### 4.1 系统架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              前端应用 (React)                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ 市场列表页   │  │ 套利监控页   │  │ 设置页面     │  │ 详情弹窗     │    │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘    │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                     状态管理 (Zustand)                               │   │
│  │  - 市场数据缓存  - 套利机会列表  - 用户配置  - API Key管理           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ HTTP/REST
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Node.js 代理服务器                                  │
│  ┌─────────────────────────────┐  ┌─────────────────────────────────────┐  │
│  │   Predict.fun API 路由      │  │   Polymarket API 路由               │  │
│  │   /api/predict/*            │  │   /api/polymarket/*                 │  │
│  └─────────────────────────────┘  └─────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 技术栈

#### 前端
| 类别 | 技术 | 版本 |
|------|------|------|
| 框架 | React | 18+ |
| 语言 | TypeScript | 5.2+ |
| 构建工具 | Vite | 5.0+ |
| 状态管理 | Zustand | 4.4+ |
| 数据获取 | TanStack Query | 5.0+ |
| UI组件库 | shadcn/ui + Tailwind | - |
| 图表库 | Recharts | 2.10+ |
| 表格组件 | TanStack Table | 8.10+ |

#### 后端
| 类别 | 技术 | 版本 |
|------|------|------|
| 运行时 | Node.js | 20+ |
| 框架 | Fastify | - |
| 语言 | TypeScript | 5.2+ |
| 缓存 | node-cache | - |

### 4.3 API集成

#### Predict.fun API
| 端点 | 用途 | 限制 |
|------|------|------|
| `GET /v1/markets` | 获取市场列表 | first ≤ 150 |
| `GET /v1/markets/{id}/orderbook` | 获取订单簿 | - |
| `GET /v1/search` | 搜索市场 | - |

#### Polymarket API
| 端点 | 用途 | 来源 |
|------|------|------|
| `GET /markets` | 获取市场列表 | Gamma API |
| `GET /book` | 获取订单簿 | CLOB API |
| `GET /prices-history` | 获取价格历史 | CLOB API |

#### API限制对比
| 限制项 | Predict.fun | Polymarket |
|--------|-------------|------------|
| 速率限制 | 240 req/min | 100 req/min |
| 分页限制 | first ≤ 150 | limit ≤ 100 |
| 认证要求 | 必需 | 部分端点需要 |
| CORS支持 | 否（需要代理） | 否（需要代理） |

### 4.4 实时更新策略

| 数据类型 | Predict.fun | Polymarket | 说明 |
|----------|-------------|------------|------|
| 市场列表 | 30秒轮询 | 30秒轮询 | 市场信息变化不频繁 |
| 订单簿 | 10秒轮询 | WebSocket | 订单簿变化频繁 |
| 套利计算 | 5秒 | 5秒 | 基于最新数据实时计算 |

### 4.5 数据模型

#### 统一市场模型
```typescript
interface UnifiedMarket {
  id: string;                   // 组合ID
  source: 'predict' | 'polymarket';
  conditionId: string;          // 用于跨平台匹配
  categorySlug?: string;        // 多元市场组标识
  title: string;
  isActive: boolean;
  isTradable: boolean;
  yesPrice: number;             // 0-1
  noPrice: number;              // 计算: 1 - yesPrice
  volume24h?: number;
  liquidity?: number;
  lastUpdated: number;
  feeRate: number;
}
```

#### 套利机会模型
```typescript
interface ArbitrageOpportunity {
  id: string;
  conditionId: string;
  predictMarket: UnifiedMarket;
  polymarketMarket: UnifiedMarket;
  direction: 'predict_to_polymarket' | 'polymarket_to_predict';
  roi: number;
  netProfit: number;
  confidence: 'high' | 'medium' | 'low';
  detectedAt: number;
}
```

---

## 五、已知问题与解决方案

### 5.1 问题清单

| # | 问题 | 影响 | 解决方案 |
|---|------|------|----------|
| 1 | Predict.fun API CORS限制 | 浏览器无法直接调用 | 搭建Node.js代理服务器 |
| 2 | API Key存储安全 | 前端存储有风险 | localStorage + 代理转发 |
| 3 | 分页限制 (first ≤ 150) | 需要多次请求获取全部数据 | 使用cursor循环获取 |
| 4 | 无单次成交量数据 | 无法精确计算套利规模 | 使用订单簿深度估算 |
| 5 | No价格需计算 | 增加计算复杂度 | 封装计算函数 `1 - yesPrice` |
| 6 | 市场状态判断复杂 | 需要检查多个字段 | 封装 `isTradable()` 函数 |
| 7 | 多元市场识别 | 需要关联多个市场 | 使用 `categorySlug` 字段 |
| 8 | 速率限制 (240 req/min) | 频繁请求会被限制 | 实现请求队列和重试机制 |

### 5.2 关键代码示例

```typescript
// 市场状态判断
function isTradable(market: PredictMarket): boolean {
  return market.status === 'REGISTERED' && market.tradingStatus === 'OPEN';
}

// No价格计算
function calculateNoPrice(yesPrice: number): number {
  return Math.max(0, Math.min(1, 1 - yesPrice));
}

// 分页获取全部数据
async function fetchAllMarkets(): Promise<Market[]> {
  const allMarkets: Market[] = [];
  let cursor: string | undefined;
  
  do {
    const response = await apiClient.get('/markets', {
      params: { first: 150, after: cursor }
    });
    allMarkets.push(...response.data.data);
    cursor = response.data.cursor;
    if (cursor) await sleep(100);  // 避免速率限制
  } while (cursor);
  
  return allMarkets;
}
```

---

## 六、开发计划

### 6.1 时间估算

| 模块 | 预估时间 | 优先级 |
|------|----------|--------|
| 项目搭建 & 配置 | 4h | P0 |
| 后端代理服务器 | 6h | P0 |
| API客户端封装 | 4h | P0 |
| 市场列表页面 | 8h | P0 |
| 套利监控页面 | 10h | P0 |
| 设置页面 | 4h | P1 |
| 数据可视化 | 6h | P1 |
| 测试 & 优化 | 8h | P1 |
| **总计** | **~50h** | - |

### 6.2 里程碑

| 阶段 | 内容 | 预计完成 |
|------|------|----------|
| **MVP** | P0功能完成，基础监控可用 | Week 1-2 |
| **V1.0** | P1功能完成，增强体验 | Week 3 |
| **V1.5** | P2功能完成，数据分析 | Week 4 |

---

## 七、待确认事项

请确认以下内容，以便我们开始开发：

### 7.1 功能范围确认
- [ ] P0功能是否满足MVP需求？
- [ ] 是否需要P1/P2功能？
- [ ] 套利阈值默认值是否合适？

### 7.2 界面设计确认
- [ ] 整体布局是否符合预期？
- [ ] 套利机会分级展示方式是否满意？
- [ ] 是否需要其他页面或功能？

### 7.3 技术方案确认
- [ ] 技术栈选择是否有偏好？
- [ ] 部署平台是否有要求？
- [ ] 是否需要额外的安全考虑？

### 7.4 其他问题
- [ ] 是否有特定的市场类别优先关注？
- [ ] 是否需要与其他系统集成？
- [ ] 是否有品牌/视觉设计规范需要遵循？

---

## 八、附录

### 8.1 相关文档
- 详细功能需求: `/mnt/okcomputer/output/arbitrage_monitor_requirements.md`
- UI设计规格: `/mnt/okcomputer/output/arbitrage_monitor_ui_spec.md`
- 技术规格: `/mnt/okcomputer/output/arbitrage_monitor_tech_spec.md`

### 8.2 参考资源
- [Predict.fun API文档](https://api.predict.fun/docs)
- [Polymarket API文档](https://docs.polymarket.com/)

---

*文档版本: 1.0.0*
*最后更新: 2025年*
